import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  inputs: {
    display: "flex",
    padding: "1rem",
  },
  pickable: {
    display: "flex",
    padding: "1rem",
  },
  wrapper: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
});
