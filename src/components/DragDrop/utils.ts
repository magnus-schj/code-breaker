import { makeStyles } from "@mui/material/styles";

export const useStyles = makeStyles({
  inputs: {
    display: "flex",
    padding: "1rem",
  },
  pickable: {
    display: "flex",
    padding: "1rem",
  },
});
