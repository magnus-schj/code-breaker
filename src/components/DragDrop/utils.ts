import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  inputs: {
    display: "flex",
    padding: "1rem",
  },
  pickable: {
    display: "flex",
    padding: "1rem",
  },
});
